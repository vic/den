---
title: Configure Aspects
description: Owned configs, includes, provides, and den.default.
---

import { Aside } from '@astrojs/starlight/components';

<Aside title="Source" icon="github">
[`modules/aspects/`](https://github.com/vic/den/tree/main/modules/aspects) · Tests: [`features/user-host-bidirectional-config.nix`](https://github.com/vic/den/blob/main/templates/ci/modules/features/user-host-bidirectional-config.nix)
</Aside>

# Den created aspects.

An aspect is an attrset with per-class modules.

<Aside icon="nix" title="Nix Classes">

An flake-aspect (used by Den) is just an attrset that contains modules of different Nix `class`.

```nix
gaming = { nixos = ...; homeManager = ...; hjem = ...; }
```

Such an attrset is said to be [**Dendritic**](https://dendrix.oeiuwq.com/Dendritic.html) because it allows configuring a single cross-cutting concern (`gaming`) over different Nix classes.

Nix classes are NOT an artificial concept created by Den. There are [many nix classes in the wild](https://github.com/search?q=language%3ANix+%28%22+class+%3D+%22+AND+%22evalModules%22%29&type=code), the most common are [`nixos`](https://github.com/search?q=repo%3ANixOS%2Fnixpkgs+%22class+%3D+%5C%22nixos%5C%22%22&type=code), [`darwin`](https://github.com/nix-darwin/nix-darwin/blob/da529ac9e46f25ed5616fd634079a5f3c579135f/eval-config.nix#L81), [`homeManager`](https://github.com/nix-community/home-manager/blob/5be5d8245cbc7bc0c09fbb5f38f23f223c543f85/nixos/common.nix#L27).
</Aside>

Den **automatically** creates an aspect for each of your host/user/home defintions. For example, with the following definitions:

```nix "homeManager" "hjem" "maid" "x86_64-linux"
den.hosts.x86_64-linux.igloo.users = {
  tux = { 
    classes = [ "homeManager" "hjem" ];
  };
  pingu = {
    classes = [ "maid" ];
  };
};
```

Den will create the following **parametric** aspects for you:

```nix "nixos" "homeManager" "hjem" "maid"
 den.aspects.igloo = parametric {
   nixos = {}; # `host.class` was derived from the host platform
 };

 den.aspects.tux = parametric {
   homeManager = {};
   hjem = {};
 };

 den.aspects.pingu = parametric {
  maid = {};
 };
```

## Aspect Basics

In addition to class modules, an aspect has an `includes` list referencing other aspects, and `provides` for sub-aspects in same category:

```nix
{ den, ... }: {
  den.aspects.laptop = {
    # Owned configs per class
    nixos = { pkgs, ... }: {
      environment.systemPackages = [ pkgs.git ];
    };
    darwin.nix-homebrew.enable = true;
    homeManager.programs.starship.enable = true;

    # Dependencies
    includes = [
      den.aspects.dev-tools
      den.provides.primary-user
    ];

    # Named sub-aspects
    provides.gpu = { host, ... }:
      lib.optionalAttrs (host ? gpu && host.gpu == "nvidia") {
        nixos.hardware.nvidia.enable = true;
      };
  };
}
```

## Owned Configs

Attributes named after a Nix class (`nixos`, `darwin`, `homeManager`, or any
custom class) are **owned configs**. They can be:

- **Attrsets**: merged directly into the class configuration.
- **Functions**: called with the class's module arguments (`{ config, pkgs, lib, ... }`).

```nix
# Attrset form
den.aspects.laptop.nixos.networking.hostName = "laptop";

# Function form
den.aspects.laptop.nixos = { pkgs, ... }: {
  environment.systemPackages = [ pkgs.git ];
};
```

## Includes

`includes` is a list of aspects, attrsets, or parametric functions:

```nix
den.aspects.workstation.includes = [
  # Reference another aspect (its full DAG is included)
  den.aspects.dev-tools

  # Static attrset (always applied)
  { nixos.programs.vim.enable = true; }

  # Parametric function (applied when context shape matches)
  ({ host, ... }: { nixos.networking.hostName = host.hostName; })

  # Battery
  (den.provides.user-shell "fish")
];
```

## Provides

`provides` creates named sub-aspects accessible via `den.aspects.<name>._.<sub>`
or `den.aspects.<name>.provides.<sub>`:

```nix
den.aspects.tools.provides.editors = {
  homeManager.programs.helix.enable = true;
  homeManager.programs.vim.enable = true;
};

# Used elsewhere:
den.aspects.alice.includes = [ den.aspects.tools._.editors ];
```

`provides` can also be parametric functions:

```nix
den.aspects.alice.provides.work-vpn = { host, user, ... }:
  lib.optionalAttrs host.hasVpn {
    nixos.services.openvpn.servers.work.config = "...";
  };
```

## Global Defaults

`den.default` is a special aspect applied to every host, user, and home:

```nix
{
  den.default = {
    nixos.system.stateVersion = "25.11";
    homeManager.home.stateVersion = "25.11";
    includes = [
      den.provides.define-user
      den.provides.inputs'
    ];
  };
}
```

<Aside type="caution">
Owned configs from `den.default` are deduplicated across pipeline stages.
Parametric functions in `den.default.includes` are evaluated at every context
stage. Use `den.lib.take.exactly` if a function should only run in specific contexts.
</Aside>

## Bidirectional Flow

Users configure their hosts. Hosts configure their users. Aspects flow
in both directions:

```nix
# User aspect contributes to host's NixOS config
den.aspects.alice = {
  nixos.users.users.alice.extraGroups = [ "docker" ];
  homeManager = { pkgs, ... }: { home.packages = [ pkgs.htop ]; };
};

# Host aspect contributes to all its users' home config
den.aspects.laptop = {
  homeManager.programs.ssh.enable = true;
  nixos.services.openssh.enable = true;
};
```
